/* routes */
/* routes */
interface RouteProps {
  name: string;
  component: any;
  title?: string;
  isLazy?: boolean;
  isHide: boolean;
  accessLabel: number | string;
}

export type {RouteProps};
